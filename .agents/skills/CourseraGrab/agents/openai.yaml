interface:
  display_name: "CourseraGrab"
  short_description: "Repo-specific patterns and workflows for CourseraGrab"
  default_prompt: "Use the CourseraGrab repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true